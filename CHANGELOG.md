# 변경 이력 (Release Notes)

> 수정 요청이 반영될 때마다 이 파일에 항목을 추가하고 버전을 올립니다.
> 버전 규칙: `주.부.수정` (기능 추가 = 부 올림, 버그/조정 = 수정 올림).

## [1.11.4] - 2026-08-12
### 변경
- **나머지 창들도 같은 외곽으로 맞췄습니다.** `공에 그리기` 창과 업데이트 노트 팝업이
  설정창·진행창과 똑같은 둥근 모서리·1px 테두리·그림자로 보입니다. Windows 10 에서도 동일합니다.

> 슬라임·조준선·파티클·점수판처럼 화면에 겹쳐 그리는 창들은 배경이 투명해야 해서 대상이 아닙니다.
> 알림(토스트)은 서서히 사라지는 효과 때문에 투명 창이어야 하지만, 이미 같은 모양(둥근 모서리 +
> 1px 테두리 + 그림자)이라 그대로 뒀습니다.

## [1.11.3] - 2026-08-12
### 수정
- **Windows 10 에서 창 모서리가 각지던 문제.** 1.11.2 에서 창 외곽을 OS(DWM) 에 맡기면서,
  둥근 모서리가 Windows 11 에서만 나오고 Windows 10 에서는 각지게 바뀌었습니다.
  OS 를 보고 갈라지도록 고쳤습니다 — Windows 11 은 OS 그림자를 그대로 쓰고,
  Windows 10 은 1.11.1 까지 쓰던 방식(앱이 직접 둥글게 자르기)으로 되돌아갑니다.
  두 경우 모두 둥근 모서리 + 같은 톤의 1px 테두리로 보입니다.

## [1.11.2] - 2026-08-12
### 변경
- **창 외곽을 Windows(DWM) 가 직접 그리도록 바꿨습니다.** 설정창 그림자가 두껍고 진해서
  튀어 보였던 것을, 다른 앱 창과 똑같은 OS 기본 그림자로 바꿨습니다. 모서리 둥글기도
  Windows 11 기본값을 따르고, 테마와 같은 톤의 1px 테두리(#3C3C44)가 생겼습니다.
- **업데이트 진행창도 같은 외곽으로 맞췄습니다.** 설정창과 똑같이 둥근 모서리·테두리·그림자가
  붙습니다.

> Windows 10 에서는 이 OS 기능이 없어 각진 모서리로 보였는데, 1.11.3 에서 바로잡았습니다.

## [1.11.1] - 2026-08-12
### 수정
- **설정창 모서리가 사각형으로 보이던 문제.** 둥근 모서리 밖으로 타이틀바·사이드바 배경이
  삐져나와 네 귀퉁이가 각져 보였습니다. 창 외곽을 둥근 영역으로 직접 잘라내 실제 화면에서도
  네 모서리가 깔끔하게 둥글게 보이도록 고쳤습니다. 그림자도 둥근 외곽선을 따라갑니다.

## [1.11.0] - 2026-08-12
### 추가
- **설정에 `업데이트 노트` 탭.** 버전별 릴리스 노트를 최신순으로 쭉 보여줍니다.
  지금 쓰는 버전에는 `사용 중` 표시가 붙습니다. `새로고침` 으로 다시 받아올 수 있습니다.
  (기존 `최근 변경 내용 보기` 버튼과 팝업 창은 이 탭으로 대체됐습니다.)
- **공 우클릭 메뉴에 현재 버전 표시.** 메뉴 아래쪽에 `ThrowMe vX.Y.Z` 가 보입니다.

## [1.10.1] - 2026-08-12
### 수정
- **업데이트 진행창 문구가 잘리던 문제.** "vX.Y.Z 로 업데이트하고 있습니다. 끝나면 자동으로
  다시 시작합니다." 를 한 줄에 넣어 창 폭에서 잘렸습니다. 두 줄로 나눠 보여줍니다.

## [1.10.0] - 2026-08-12
### 변경
- **업데이트가 조용해졌습니다.** 앱을 켤 때 새 버전이 있으면 **묻지 않고** 진행바만 잠깐 띄워
  받아서 적용하고 자동으로 다시 시작합니다. "지금 재시작할까요?" 확인창과 업데이트 직후
  변경 내용 팝업을 없앴습니다.
- **변경 내용은 설정에서 봅니다.** 설정 → 일반 → `최근 변경 내용 보기`.
  업데이트 직후에는 방금 적용된 버전의 노트를 보여주고, 그 외에는 최신 릴리스 노트를 가져옵니다.
  관련 설정 토글(`변경 내용 알리기`, `새 버전 받으면 바로 물어보기`)은 필요 없어져 제거했습니다.

### 수정
- **다른 PC로 던진 공이 사라져 앱을 다시 켜야 하던 문제.**
  공을 넘길 때 클라이언트는 공을 숨기고 물리를 멈춘 뒤 서버 결과를 기다리는데, 서버가 소유자로
  인정하지 않으면 `HANDOFF_RESULT` 대신 **`ERROR` 만** 보냈고 클라이언트는 그걸 로그만 찍고
  무시했습니다 → 결과를 영원히 기다리며 공이 사라졌습니다.
  - 서버: `NOT_OWNER` 일 때도 `HANDOFF_RESULT(accepted=false)` 를 함께 보냅니다.
  - 클라이언트: `ERROR` 를 받으면 대기 중이던 공을 즉시 회수하고, **6초 워치독**으로
    응답이 아예 없어도 공을 되돌립니다. 회수 위치도 유효성을 검사해 작업표시줄에 갇히지 않습니다.
- **접속하지 않은 PC로 향하는 배치가 만들어지던 문제.**
  서버의 파티 순서는 나간 사람도 계속 보관하는데 방장이 그걸로 배치를 만들어, 방에 없는 PC 로
  던지는 통로가 생겼습니다. 이제 **현재 접속 중인 멤버만으로** 배치를 만들고,
  `relay.json` 에 남아 있던 예전 방의 링크도 걸러냅니다.
- **멀티 PC 진단이 불가능하던 문제.** 릴레이가 실패만 기록해 로그로 원인을 볼 수 없었습니다.
  이제 방 상태(방장·순서·접속자)와 핸드오프 송수신·회수 사유를 남깁니다.

## [1.9.2] - 2026-07-29
### 변경
- **설정창을 여는 동안에는 공 주변 클릭 여유를 없앱니다.** 공 근처를 넉넉히 클릭할 수 있게 해 둔
  여유(기본 28px)가, 공이 날아갈 때 보이지 않는 원이 되어 뒤에 있는 설정창의 클릭을 가렸습니다.
  이제 설정창이 열려 있는 동안에는 **공 그림만큼만** 클릭을 받습니다.
  설정창을 닫으면 원래 여유로 돌아옵니다.

## [1.9.1] - 2026-07-29
### 변경
- **재시작을 묻기 전에 변경 내용을 먼저 보여줍니다.** 예전에는 "새 버전 받았습니다, 지금 재시작할까요?"
  만 묻고, 무엇이 바뀌는지는 재시작한 뒤에야 알 수 있었습니다. 순서가 거꾸로였습니다.
  이제 **확인창에서 바로 변경 내용을 읽고** 재시작 여부를 정할 수 있습니다.
  여기서 이미 읽었다면 재시작 후 같은 내용이 또 뜨지 않습니다.

## [1.9.0] - 2026-07-29
### 수정
- **업데이트 후 변경 내용 팝업이 뜨지 않던 문제.** 팝업을 "할 일이 없을 때"(ApplicationIdle) 띄우도록
  예약했는데, 공이 움직이는 동안에는 물리 루프가 계속 돌아 그 시점이 오지 않았습니다.
  그래서 업데이트 직후 공을 받아 움직이기 시작하면 팝업이 영영 뜨지 않았습니다.
  이제 짧은 타이머로 확실히 띄웁니다.
- **종료 중 오류가 반복 기록되던 문제.** 앱을 닫는 도중 다른 PC에서 공이 넘어오면
  이미 닫힌 창을 다시 띄우려다 오류가 났습니다.

### 변경
- **여러 버전을 건너뛰면 그 사이 내용을 모두 보여줍니다.** 예전에는 최신 릴리스 하나의 내용만
  담겨서, 1.5.0 에서 1.8.0 으로 한 번에 올라가면 1.6.0·1.7.0 의 변경 내용이 사라졌습니다.
  이제 건너뛴 버전들을 최신순으로 모아 한 팝업에 보여줍니다.

## [1.8.0] - 2026-07-28
### 수정
- **공이 날아가는 동안 다른 창이 클릭되지 않던 문제.** 공은 112px 인데 창은 그 4배(446px)였고,
  넓게 낚아채려고 **창 전체가 클릭을 받도록** 되어 있었습니다. 그래서 보이지 않는 사각형이
  공을 따라다니며 뒤에 있는 창의 클릭을 삼켰습니다(설정창 탭이 안 눌리는 증상).
  이제 **공 주변 원형**으로만 클릭을 받습니다. 범위는 설정 → 일반 → `클릭 여유` 에서 조절합니다.

### 추가
- **날아가는 공을 클릭하면 되치기.** 예전에는 움직이는 공을 클릭하면 그 자리에서 멈췄습니다.
  이제 **속도를 유지한 채 클릭한 반대 방향으로 튕겨** 나갑니다. 라켓으로 치듯 방향을 바꿀 수 있고,
  빠른 공을 되칠수록 이펙트도 커집니다.
- **작업표시줄에 표시.** 작업표시줄과 Alt+Tab 목록에 ThrowMe 가 나옵니다.
  설정 → 일반 → `작업표시줄에 표시` 로 끌 수 있습니다.
- **공이 넘어갈 때 우측 하단 알림.** 공이 다른 PC로 넘어가면 **누구에게 갔는지** 알려줍니다.
  그동안은 공이 상대 PC로 가면 화면에서 그냥 사라져서 앱이 죽은 것처럼 보였습니다.
  공이 돌아올 때도 알려줍니다. 설정 → 일반 → `알림 표시` 로 끌 수 있습니다.

## [1.7.0] - 2026-07-28
### 추가
- **앱 아이콘.** 보라 배경에 날아가는 민트 슬라임 아이콘을 넣었습니다.
  실행 파일(탐색기·작업표시줄·바로가기), 트레이 아이콘, 설정창 등 창 아이콘에 모두 적용됩니다.
  - 16 · 20 · 24 · 32 · 40 · 48 · 64 · 128 · 256px 프레임을 담아 어느 크기에서도 선명합니다.
  - 원본 이미지의 둥근 모서리 바깥이 검정이라 그대로 쓰면 작업표시줄에 검은 사각형이 보입니다.
    둥근 사각형 알파 마스크를 씌워 모서리를 투명하게 처리했습니다.
  - 리소스를 못 읽는 경우에만 기존처럼 코드로 그린 아이콘으로 대체합니다.

### 수정
- csproj 주석의 한글이 깨져 있던 것을 고쳤습니다(동작 영향 없음).

## [1.6.0] - 2026-07-27
### 변경
- **앱 이름이 `Slimey` → `ThrowMe` 로 바뀌었습니다.** 창 제목·트레이 아이콘·설정창 등
  눈에 보이는 모든 이름과, 내부 네임스페이스·어셈블리·실행 파일명(`ThrowMe.exe`)까지 함께 바꿨습니다.
- **설정·커스텀 스킨·방 접속 정보는 그대로 유지됩니다.** 데이터 폴더도
  `%APPDATA%\ThrowMe`, `%LOCALAPPDATA%\ThrowMe` 로 옮겼지만, 첫 실행 때 예전 폴더가 있으면
  자동으로 복사해 옵니다(`AppPaths`). 예전 폴더는 지우지 않으므로 문제가 생겨도 원본이 남습니다.
- **멀티 PC 연결은 영향받지 않습니다.** 릴레이 서버 주소(`slimey-relay.throwme.workers.dev`)는
  그대로 두었습니다 — 바꾸면 아직 업데이트하지 않은 PC 와 연결이 끊기기 때문입니다.

## [1.5.1] - 2026-07-27
### 수정
- **업데이트 알림 팝업에서 인용문이 깨져 보이던 문제.** 릴리스 노트의
  `> ⚠️ ...` 안내 문구가 해석되지 않아 꺾쇠 기호가 글자로 그대로 노출됐습니다.
  이제 왼쪽에 강조색 세로선을 둔 안내 블록으로 표시합니다.
- **여러 줄로 쓴 항목이 목록 밖으로 떨어져 나오던 문제.** 한 항목을 두세 줄로
  접어 쓰면 이어지는 줄이 별도 문단이 되어 글머리표 밖에 왼쪽 정렬로 붙었습니다.
  이제 해당 항목 본문으로 이어 붙습니다.

## [1.5.0] - 2026-07-27
### 추가
- **방장 위임.** 파티원 카드를 **우클릭 → 방장 위임**. 접속 중인 사람에게만 넘길 수 있습니다.
  위임한 뒤에는 방을 만든 사람이 다시 들어와도 방장을 자동 회수하지 않습니다(위임 의사 존중).
- **방 공통 테마.** 방장이 `방 공통 테마`를 고르면 **방에 있는 모든 PC의 테마가 같이 바뀝니다.**
  나중에 입장한 사람도 들어오는 즉시 방 테마로 맞춰집니다.

### 변경
- **배치를 도형으로 표시.** 파티원을 세로 목록 대신 **모니터 카드가 좌 → 우로 이어진 형태**로
  보여줍니다. 카드 사이의 `↔` 선이 공이 지나가는 통로이고, 공을 가진 PC의 카드에는 점이 표시됩니다.
  드래그로 순서를 바꾸는 방식은 그대로입니다.

### 수정
- **방을 만든 사람이 방장을 빼앗기던 문제.** 개설자의 연결이 잠깐 끊기면(앱 재시작·네트워크 순단)
  방장이 다른 사람에게 승계된 뒤 돌아와도 되돌아오지 않았습니다. 이제 서버가 **개설자를 따로 기억**해
  돌아오면 방장을 회복시킵니다(명시적으로 위임한 경우는 제외).
- **모두 나간 뒤에도 파티원 목록에 사람이 남던 문제.** 방을 나갈 때 멤버·순서·방장 정보를 비웁니다.
- **공이 없는 상태로 방을 나가면 공이 돌아오지 않던 문제.** 공을 남에게 넘긴 직후에는 공 좌표가
  화면 밖(넘어가던 위치)이라, 방을 나가며 공을 되찾아도 화면 밖에 놓여 보이지 않았고 물리도 멈춰
  앱을 다시 켜야 했습니다. 이제 되찾을 때 **위치가 유효한지 검사해** 화면 밖이면 중앙으로 되돌립니다.
  서버 연결이 끊길 때도 같은 처리를 합니다.
- **공이 빠를 때 렉이 걸리던 문제.**
  - 창 이동을 WPF `Left/Top` 대신 **Win32 `SetWindowPos`** 로 바꿨습니다. 투명(레이어드) 창에서
    `Left/Top` 세터는 의존 속성 변경 → 레이아웃 → 재합성을 매 프레임 유발합니다. 위치가 그대로면
    아예 호출하지 않습니다.
  - 파티클·타격 문구 오버레이가 **표시할 게 없어도 매 프레임 풀 전체를 순회**하던 것을 조기 반환으로
    막았습니다. 공이 움직이는 내내 렌더 루프가 도는 만큼 누적 비용이 컸습니다.

## [1.4.0] - 2026-07-27
### 추가
- **업데이트 후 변경 내용 팝업.** 자동 업데이트가 적용되어 새 버전이 처음 실행될 때,
  무엇이 달라졌는지 알려주는 팝업이 한 번 뜹니다. 그동안은 조용히 교체만 되어서
  사용자가 새 기능이 생긴 줄 모르고 지나가는 문제가 있었습니다.
  - 내용은 **GitHub 릴리스 노트 본문**을 그대로 보여줍니다. 업데이트 확인 시 이미 받아오던
    응답에 들어 있어 추가 통신이 없습니다.
  - 제목(`##`)·불릿(`-`, 중첩 포함)·**굵게**·인라인 코드 정도의 마크다운을 해석합니다
    (외부 마크다운 패키지 없음).
  - **적용된 버전과 실행 중인 버전이 같을 때만** 뜨고, 한 번 보면 다시 뜨지 않습니다.
- **설정 → 일반 → 업데이트**에 `변경 내용 알리기` 토글과 `최근 변경 내용 보기` 버튼 추가.
  팝업 안에서도 `다음부터 알리지 않기`로 끌 수 있고, 끈 상태여도 노트는 소비되어
  다음 업데이트 때 옛 내용이 뜨지 않습니다.
## [1.3.1] - 2026-07-27
### 수정
- **골이 들어갔는데 득점이 안 되던 문제 수정(농구).** 원인이 세 개 겹쳐 있었습니다.
  - **판정 X 좌표를 프레임 끝에서 읽었음.** 공이 림 높이를 지나는 *순간*이 아니라 그 프레임이
    끝난 위치로 폭을 재고 있었습니다. 그래서 좌우 속도가 빠른 슛(비스듬히 넣는 슛)은
    판정 시점에 공이 이미 개구부를 지나쳐 있어 골이 누락됐습니다. 정중앙으로 깨끗이 넣은
    슛조차 빠르면 탈락할 수 있었습니다. → 림 높이 교차 지점의 X 를 보간해서 판정합니다.
  - **`vel.Y > 0`(하강 중) 조건이 림 충돌에 뒤집혔음.** 림을 살짝 스치며 들어간 공은
    통과 프레임에서 속도가 위로 반사되어 조건이 깨졌습니다. → 위치 변화량(dy)으로 하강을
    판정하므로 스치고 들어간 골도 인정됩니다.
  - **인정 폭이 너무 좁았음(0.17S).** 겹침 없이 지나는 이론폭(≈0.18S)에 맞춰져 있었는데,
    빠른 공은 프레임 단위 이동 탓에 림 높이에서 이미 살짝 겹친 상태로 통과합니다(눈에는
    들어간 골). → 공 표면이 림 튜브 중심에 닿는 지점까지, **0.24S** 로 넓혔습니다.
  림에 맞고 튀어나가는 공은 교차 지점 X 가 개구부 밖이라 여전히 득점으로 세지 않습니다.

## [1.3.0] - 2026-07-27
### 추가
- **빈 방 자동 폐기(서버).** 방에 아무도 없는 상태가 **30분** 지나면 방을 폐기합니다
  (시크릿·파티 순서·배치·소유권 전부 삭제). 그 전에는 방이 영구히 남아 있어서
  ① 한 번 정한 비밀번호를 바꿀 수 없고 ② 예전에 접속했던 PC 이름이 파티 순서에 계속
  쌓이는 문제가 있었습니다. 앱을 껐다 켜는 정도(30분 이내)로는 방이 유지됩니다.
  ※ Durable Object 는 알람이 하나뿐이라, 핸드오프 ACK 타임아웃과 폐기 예약 중
  **더 이른 마감시각**으로 재예약하도록 알람을 통합했습니다.
- **방장(호스트) 시스템.** 방 코드를 처음 만든 PC가 **방장**이 되고, **배치는 방장만** 정합니다.
  다른 PC가 순서·배치를 바꾸려 하면 서버가 `NOT_HOST` 로 거부합니다. 방장이 나가면
  파티 순서상 다음 노드로 자동 승계됩니다(방장 없는 방이 생기지 않도록).
- **멀티 PC 설정에 탭 추가 — `방 생성` / `입장`.**
  - 방 생성: 방 코드(자동 생성 버튼 포함)·비밀번호·PC 이름 → 내가 방장이 됩니다.
  - 입장: 방장에게 받은 방 코드·비밀번호·PC 이름을 입력해 참여합니다.
- **파티원 목록.** 방에 들어오면 참여자가 표시되고, **방장이 드래그로 좌 → 우 순서**를
  정합니다. 목록에 방장·공 소유 표시가 함께 나옵니다.

### 변경
- **배치를 좌우 전용으로 단순화.** 위/아래 매핑과 엣지별 수동 설정 UI를 없애고,
  **파티 순서 하나**로 배치를 정하도록 바꿨습니다. 순서 [A, B, C] 는
  `A.Right↔B.Left`, `B.Right↔C.Left` 체인으로 자동 변환됩니다(양 끝은 벽에 튕김).

### 수정
- **다른 PC로 넘어온 공이 작업표시줄에 붙어 멈추던 문제 수정.**
  진입 위치를 엣지 법선 방향으로만 밀어 넣어서, 진입 지점이 화면 위/아래 끝에 가까우면
  공 절반이 경계 밖(작업표시줄·화면 밖)에 놓였습니다. 그러면 물리 엔진이 모든 방향을 벽으로
  판정해 그 자리에 갇혔습니다. 이제 ① 접선 방향으로도 공 반지름만큼 여유를 두고,
  ② **작업영역 기준 엄격 판정**으로 유효한 자리를 찾아(없으면 안쪽으로 더 들어가며 탐색)
  공을 놓습니다. 놓을 자리가 전혀 없으면 수락을 거부해 보낸 쪽이 반사합니다(공 유실 없음).

## [1.2.0] - 2026-07-27
### 추가
- **릴레이 서버 배포 완료.** 멀티 PC 공 전달이 실제 인터넷 경유로 동작합니다.
  Cloudflare Workers + Durable Objects, 무료 티어. 방 생성·시크릿 인증·프레즌스·
  핸드오프 라우팅·소유권 검증(비소유자 거부)까지 서버 검증 10/10 통과.

### 변경
- **서버 주소를 앱에 내장**(`AuthService.DefaultServerBaseUrl`). 설정 창에서 서버 주소
  입력란을 없앴습니다 — 모든 PC가 같은 값을 쓰는데 매번 입력하면 오타 위험만 커지기 때문입니다.
  이제 멀티 PC 설정은 **방 코드 + 시크릿 + 이 PC 이름**만 입력하면 됩니다.
  개발용 로컬 서버(`ws://127.0.0.1:8787`)로 바꾸려면 `relay.json` 의 `serverBaseUrl`
  또는 실행 인자로 덮어쓸 수 있습니다(UI에는 노출하지 않음).

## [1.1.1] - 2026-07-27
### 수정
- **타격 문구·파티클이 엉뚱한 곳(화면 한복판)에 뜨던 문제 수정.**
  이펙트를 물리 계산이 **끝난 뒤**의 공 위치에 띄우고 있어서, 공이 빠를수록 실제 벽 접점에서
  크게 빗나갔습니다(최고속에서 한 프레임 이동량 = 최대 2000px). 이제 물리 엔진이 **충돌이 일어난
  순간의 위치**(`PhysicsStepResult.CollisionPosition`)를 함께 돌려주고, 문구·파티클·쿠션 스파크가
  모두 그 지점에 발생합니다.
- **멀리 떨어진 이펙트가 잘려 안 보이던 문제 수정.** 문구/파티클 오버레이는 성능을 위해
  작은 고정 크기 창 하나로 그리는데, 창을 활성 이펙트들의 **무게중심**에 놓고 있어서 공이 양쪽 벽을
  번갈아 때리면 창이 그 사이(화면 한복판)로 가버려 양쪽 이펙트가 모두 창 밖으로 잘렸습니다.
  → 창을 **경계 중심**에 놓고, 새 이펙트가 창 범위를 벗어나게 만들면 오래된 것을 비워
  "활성 이펙트는 항상 창 안"을 보장합니다. (창 리사이즈는 레이어드 창에서 동기 스톨을 유발하므로 쓰지 않음.)

### 변경
- **던지기 가중치 범위 확대** 10.0x → **20.0x**. 속도 상한도 함께 상향(`MaxThrowSpeed`/`MaxSpeed` 20000 → 40000 px/s).
  최고속에서도 substep 터널링 방지가 유지됨을 확인(400프레임 × 40000px/s 전부 화면 안).

## [1.1.0] - 2026-07-27
### 추가
- **빠르게 숨기기 단축키**(기본 `Alt + \``) — 슬라임·놓아둔 당구공·이펙트 오버레이를 한 번에 감추고,
  같은 키를 다시 누르면 되돌아옵니다. 설정창 '단축키' 탭에서 키/마우스 조합으로 재설정 가능.
- **테마별 커스텀 이미지** — 슬라임·당구공 테마에 내 이미지를 덧씌울 수 있습니다.
  - **불러오기**: PNG/JPG 등을 고르면 앱 저장소로 복사해 보관(원본을 옮기거나 지워도 안 깨짐).
  - **그리기**: 앱 안에서 직접 그려 공에 올림(10색 팔레트·굵기·지우개·되돌리기·배경 투명/흰색).
  - 이미지는 공 원형으로 잘리고 구면 음영·광택이 덧씌워져 스티커가 아니라 공 표면처럼 보입니다.
  - 테마별로 따로 저장되며, 덧씌우기 on/off 와 이미지 크기(30~160%)를 조절할 수 있습니다.
  - 몬스터볼·하이퍼볼·마스터볼은 고유 디자인 유지를 위해 제외.

### 변경
- **던지기 가중치 범위 확대** 2.0x → **10.0x**. 실제로 체감되도록 속도 상한도 함께 상향
  (`MaxThrowSpeed`/`MaxSpeed` 7000 → 20000 px/s).
- 충돌 이펙트(소리·파티클·찌그러짐·표정) 기준값을 속도 상한과 **분리**(`ImpactReferenceSpeed` = 7000).
  상한을 올려도 이펙트가 터지는 임계는 이전과 동일하게 유지됩니다.

## [1.0.2] - 2026-07-24
### 수정
- 3/4구를 놓은 상태로 테마를 바꾸면 당구공이 안 치워지던 문제 수정 — 테마 변경 시 함께 정리.
- 설정창 스크롤바 재디자인(얇고 둥근 썸, 화살표 제거) + 모서리 곡률 정리.
- 일반 탭 '위치 초기화' 버튼 크기를 여유있게 조정.
- UI 폰트를 Pretendard(맑은 고딕 폴백)로 변경.

## [1.0.1] - 2026-07-24
### 변경
- 저장소를 공개(Public)로 전환 — 누구나 `git clone` 가능.
- 자동 업데이트를 **토큰 없이** 동작하도록 정리(공개 릴리스 직링크로 다운로드). 배포 exe에 토큰을 넣지 않음.

## [1.0.0] - 2026-07-24
### 최초 릴리스
- **데스크톱 슬라임**: 투명 창의 슬라임을 드래그·던지기, 관성/마찰/모서리 반사(당구 쿠션식), 멀티 모니터(음수 좌표·간격·DPI) 대응.
- **스킨**: 젤리 / 당구공(기본 8번 검정, 4·3구 시 흰 수구) / 몬스터볼·하이퍼볼·마스터볼(클릭 열림).
- **스핀**: 드래그 컬로 사이드 스핀(마그누스 곡선) 충전 — 드래그 중 유지, 던질 때 소모.
- **큐대 모드**(당구공): 공 주위 클릭→당겨서 발사. 스핀 점으로 세로=끌어치기/밀어치기, 가로=사이드.
  - 아래(6시) 끌어치기 = 앞으로 갔다가 되돌아옴, 위(12시) 밀어치기 = 더 밀고 나감.
- **타격 문구**: 젤리가 벽에 부딪히거나 맞으면 "Hit!/Pow!/Bonk!" 등 메이플식 문구가 팝업.
- **잡기 단축키**: 전역 단축키/마우스 버튼으로 슬라임을 커서로 회수(기본 Ctrl+Shift+G).
- **설정 창**: 다크 2-pane, 크기/반발/말랑/던지기·테마 카드·소리·단축키.
- **자동 업데이트**: 실행 시 새 릴리스가 있으면 백그라운드로 받아 다음 실행 때 조용히 적용.
