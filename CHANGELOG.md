# 변경 이력 (Release Notes)

> 수정 요청이 반영될 때마다 이 파일에 항목을 추가하고 버전을 올립니다.
> 버전 규칙: `주.부.수정` (기능 추가 = 부 올림, 버그/조정 = 수정 올림).

## [1.27.1] - 2026-09-02
### 수정
- **작업표시줄 걷기에서 걷는 모션만 있고 제자리에 머물던 문제.** 젤리 스킨의 벽 랜덤 반사가
  바닥에 닿는 매 프레임 걸려, 가로 속도가 세로로 돌아간 뒤 착지 처리로 지워지고 있었습니다.
  자동 이동 중 바닥 접촉에는 랜덤 반사를 걸지 않습니다. 좌우로만 움직이는 건 의도한 대로입니다.
- **작업표시줄 걷기의 바닥이 작업표시줄 윗변이 됩니다.** 그동안은 화면 맨 아래까지 내려가
  작업표시줄 아이콘을 가렸습니다. 던지기는 그대로 작업표시줄 위를 지나갈 수 있습니다.

### 변경
- **`이동 속도`를 올리면 걸음 빠르기는 그대로 두고 한 걸음 거리만 늘어납니다.** 기어다니기와
  작업표시줄 걷기의 걸음 주기는 속도 17 기준으로 고정됩니다. 커서 따라가기는 이 변경과 무관하게
  예전처럼 멀수록 걸음이 빨라집니다.
- **`따라다닐 때 크기` 설정을 없앴습니다.** 커서 따라가기도 모양/물리의 `크기`를 그대로 씁니다.

## [1.27.0] - 2026-08-27
### 추가
- **`자동 이동`** (설정 → 일반 → 자동 이동). 무한 튕기기와 달리 **느리게 끊임없이** 움직입니다.
  튕겨 다니는 게 아니라 기어다닙니다. 세 가지 중에 고릅니다.
  - **기어다니기** — 화면을 느릿느릿 돌아다닙니다. 방향이 몇 초마다 조금씩 바뀝니다.
  - **작업표시줄 걷기** — 화면 아래(작업표시줄 위)에 붙어 좌우로만 걷습니다.
  - **커서 따라가기** — 마우스 커서의 **오른쪽 아래**를 목표로 천천히 따라옵니다.
- **미끄러지지 않고 정말로 기어다닙니다.** 사람이 한 발 내딛듯 **몸을 모았다가 쭉 뻗고
  다시 멈추기**를 반복합니다. 한 걸음이 약 1.15초이고, 그중 절반 가까이는 멈춰 있습니다.
  - 뻗는 순간에 맞춰 **바닥에 눌린 통통한 돔에서 꼬리가 끌리는 모양까지** 몸이 변합니다.
    이 자세들은 원본 슬라임 그림을 잘라 만든 컷을 그대로 씁니다.
  - 오른쪽 걸음과 왼쪽 걸음은 **각각 전용 컷**을 씁니다. 한 장을 좌우로 뒤집어 쓰지 않으므로,
    방향이 조금 흔들려도 그림이 번갈아 나오며 떨리지 않습니다.
  - 자동 이동을 끄면 평소의 동그란 모습으로 돌아옵니다.
- **자동 이동 중에는 마우스로 잡거나 던질 수 없습니다.** 스스로 걸어 다니는 중에 끌면
  스핀이 충전돼 놓는 순간 팽이처럼 돌아 버렸습니다. 멈추려면 `잡기` 단축키를 쓰세요
  (전역 단축키라 그대로 동작합니다).
- **`이동 속도`** — 초당 몇 픽셀씩 갈지 정합니다(기본 18). 낮출수록 꼬물꼬물 기어갑니다.
- **`돌아다닐 모니터`** — 자동 이동을 한 모니터 안에만 가둡니다. **던지기에는 적용되지 않아서**,
  손으로는 여전히 어느 모니터로든 보낼 수 있습니다.
- **`따라다닐 때 크기`** — 커서 따라가기 전용 지름(기본 400px). 평소 크기와 따로 저장됩니다.
- **`설정 열기` 단축키** (설정 → 단축키, 기본 `Space` + `Tab`). 잡기·숨기기와 달리
  **두 칸 모두 아무 키나** 넣을 수 있습니다. 앞 키를 누르고 있는 동안 뒤 키를 누르면 열립니다
  (순서대로 치는 평범한 타이핑은 가로채지 않습니다).
- **커서 따라가기 방식을 고를 수 있습니다** (`따라가는 방식`).
  - **따라오기** — 커서 쪽으로 한 걸음씩 뛰어 쫓아옵니다. 멀수록 걸음이 빨라집니다.
    같은 속도로 쭉 미끄러져 오지 않습니다.
  - **키링** — 커서에 매달려 흔들립니다. 커서를 움직이면 반대쪽으로 늘어졌다가,
    멈추면 커서 바로 아래로 모입니다.

### 변경
- **`이동 속도`를 올리면 보폭이 아니라 걸음이 빨라집니다.** 예전에는 한 번에 더 멀리 뛰어서
  기어가는 게 아니라 껑충 뛰는 모양이었습니다. 이제 한 걸음 거리는 그대로 두고 걸음을 빨리 놓습니다.
- **설정 창 콤보 상자의 글자가 흰색으로 보입니다.** 펼친 목록만 밝고 닫힌 상태는 검은 글자라
  읽을 수 없었습니다.
- **작업표시줄 걷기와 커서 따라가기에서는 슬라임 클릭이 막힙니다.** 작업 위를 돌아다니는
  자리라 클릭을 받으면 뒤에 있는 창의 조작을 삼키기 때문입니다. 그동안 설정으로 가는 길을
  안내 메시지로 계속 띄워 둡니다(`설정 열기` 단축키 또는 트레이 아이콘).
  - 잡기 단축키는 그대로 동작합니다. 전역 단축키라 클릭 막힘과 무관합니다.
- **자동 이동과 무한 튕기기는 서로 배타입니다.** 한쪽을 켜면 다른 쪽이 꺼집니다.
- 농구공·종이비행기에서는 자동 이동이 동작하지 않습니다(중력으로 바닥에 붙어 있어
  기어다니는 모양이 나오지 않습니다). 작업표시줄 걷기는 예외로, 스스로 중력을 씁니다.

## [1.26.5] - 2026-08-27
### 변경
- **기능 변경 없음.** 1.26.4에서 고친 한글 경로 자동 업데이트가 실제로 되는지 확인하려고 올린
  버전입니다. 1.26.4를 쓰고 있으면 켤 때 알아서 이 버전으로 올라갑니다.

## [1.26.4] - 2026-08-27
### 수정
- **경로에 한글이 들어 있으면 업데이트가 영원히 실패하던 문제.** 이게 그동안 특정 PC에서
  **무한 다운로드·재시작**이 반복된 진짜 원인이었습니다. 권한도 백신도 아니었습니다.
  - 새 버전으로 갈아 끼우는 일은 작은 명령 스크립트(`.cmd`)가 합니다. 그런데 `cmd.exe` 는 이
    파일을 **UTF-8 이 아니라 시스템 코드페이지(한국어 Windows는 949)** 로 읽습니다. 그래서
    `바탕화면\클로드 코드\...` 처럼 한글이 든 경로를 스크립트에 적어 두면 경로가 깨져
    복사가 실패했습니다. 폴더 권한은 멀쩡해서 원인을 찾기 어려웠습니다.
  - 이제 스크립트 본문에는 경로를 넣지 않고 **환경 변수로 넘깁니다.** 환경 변수는 유니코드로
    전달되므로 한글·공백이 섞인 어떤 경로에서도 안전합니다.
  - 실측: 한글 폴더에 둔 `1.26.0` 이 자동으로 `1.26.3` 으로 교체되는 것까지 확인했습니다.
    (고치기 전에는 같은 자리에서 `copy` 가 오류 1로 실패했습니다.)

> 이 문제로 구버전에 머물러 있던 PC는 이 수정판을 **한 번은 직접 받아 교체**해야 합니다.
> 그 다음부터는 한글 경로에서도 자동으로 올라갑니다.

## [1.26.3] - 2026-08-27
### 변경
- **업데이트를 받지 않은 이유를 로그에 남깁니다.** 그동안은 새 버전이 없거나 보류 중이면
  아무 기록 없이 조용히 지나가서, "업데이트가 안 된다"는 문의가 오면 로그로 답할 수가 없었습니다.
  이제 실행할 때마다 한 줄이 남습니다.
  - 받을 것이 없음 (최신 버전과 실행 중인 버전을 함께 표시)
  - 최신 버전을 확인하지 못함 (네트워크 또는 차단)
  - 새 버전은 있으나 이 PC에서 교체가 연속 실패해 보류 중
  - 새 버전은 있으나 실행 파일 폴더에 쓸 수 없음

> 릴리스를 막 올린 직후에 앱을 켜면 `받을 것이 없습니다` 가 남을 수 있습니다. 버전 확인에 쓰는
> 주소가 몇 분 늦게 갱신되기 때문입니다(설정의 업데이트 노트는 즉시 보입니다). 잠시 뒤 다시 켜면 받습니다.

## [1.26.2] - 2026-08-27
### 변경
- **기능 변경 없음.** 1.26.1에서 손본 자동 업데이트가 실제로 잘 받아지는지 확인하려고 올린
  버전입니다. 켜 두면 알아서 이 버전으로 올라갑니다.

## [1.26.1] - 2026-08-27
### 수정
- **업데이트 교체가 막힌 PC에서, 새 버전이 나올 때마다 무한 다운로드·재시작이 다시 시작되던 문제.**
  1.21.1에서 넣은 방어가 **실패한 버전 하나만** 기억해서, 다음 릴리스가 올라오면 "다른 버전이니
  다시 해 보자"며 방어가 풀렸습니다. 덮어쓸 수 없다는 원인은 그대로인데 릴리스를 낼 때마다
  160MB 다운로드와 재시작이 한 바퀴씩 다시 돌았습니다.
  - 이제 실패 기록을 **버전이 아니라 그 PC 단위**로 봅니다. 새 버전이 나와도 방어가 풀리지 않습니다.
  - **연속 2회** 실패하면 자동 재시도를 아예 멈추고, 직접 받아 교체하라고 안내합니다.
    (1회 실패는 지금처럼 24시간 뒤 한 번 더 해 봅니다 — 그 사이 백신 예외를 넣었을 수 있으니까요.)
  - 손으로 새 exe를 덮어쓰면 **자동 업데이트가 다시 켜집니다.**
- **덮어쓸 수 없는 폴더에서 실행 중이면 아예 받지 않습니다.** `Program Files` 처럼 쓰기가 막힌
  자리에서는 교체가 무조건 실패하는데, 그걸 모르고 160MB를 먼저 받고 있었습니다. 이제 받기 전에
  확인하고, 안 되면 **쓰기 가능한 폴더로 옮기라고 한 번 알려 줍니다.**

> 이미 이 문제에 빠져 구버전에 머물러 있는 PC는 이 수정판도 자동으로 받지 못합니다.
> 릴리스 페이지에서 `ThrowMe.exe` 를 직접 받아 그 PC의 기존 파일을 덮어써 주세요.

## [1.26.0] - 2026-08-25
### 변경
- **슬라임 크기가 화면 배율을 따라갑니다.** 4K처럼 배율이 150~200%인 화면에서는 글자와 아이콘만
  커지고 공은 실제 픽셀에 고정돼 있어서 혼자 작아 보였습니다. 이제 배율을 곱해 **어느 화면에서도
  같은 크기로** 보입니다. 크기 슬라이더는 그대로 `48~180px`이고, 이 값은 이제 **배율 100% 기준**입니다.
  - 배율 100%를 쓰고 있다면 지금과 똑같습니다.
  - 배율이 다른 모니터로 공을 넘기면 그 화면에 맞춰 크기가 바뀝니다.
  - 멀티 PC로 크기를 맞출 때도 배율 100% 기준값을 주고받으므로, 서로 화면 배율이 달라도
    각자 화면에서 같은 크기로 보입니다.
- **무한 튕기기 안내가 켜져 있는 동안 계속 떠 있습니다.** 8초 뒤 사라져서, 나중에 멈추고 싶을 때
  방법을 다시 알 길이 없었습니다. 무한 튕기기를 끄면 같이 사라지고, 안내를 클릭해 직접 내릴 수도
  있습니다. 켜 둔 채로 앱을 껐다 켜면 다시 띄웁니다.
  - 잡기 단축키를 바꾸면 안내 문구도 바뀐 단축키로 새로 뜹니다.

## [1.25.2] - 2026-08-25
### 수정
- **랜덤 반사가 진행 방향까지 뒤집던 문제(1.25.0).** 오른쪽으로 날아가던 공이 바닥을 맞고
  **왼쪽으로 되돌아오는** 일이 잦았습니다(바닥 충돌의 **26%**). 튕겼다기보다 되돌아온 것처럼
  보였습니다. 이제 벽을 따라 가던 쪽은 그대로 두고 **각도만** 흔듭니다 — 오른쪽으로 가던 공은
  바닥을 맞아도 계속 오른쪽으로 갑니다(측정: **0%**).
  - 정면으로 가깝게 맞았을 때는 따라가던 쪽이랄 게 없으므로 예전처럼 좌우 어느 쪽으로든 꺾입니다.

## [1.25.1] - 2026-08-25
### 수정
- **화면 맨 위로 끌어다 던지면 슬라임이 그 자리에 박혀 움직이지 않던 문제.** 모니터들의 세로 위치가
  서로 어긋나 있으면(예: 한 대만 19px 위로) 화면 맨 위에 **어느 모니터에도 속하지 않는 띠**가
  생기는데, 공을 놓을 때 이 띠까지 갈 수 있었습니다. 그 자리에서는 어느 방향으로도 나갈 수 없어
  공이 굳은 채 튕기지도 떨어지지도 않았고, 잡아서 옮기기 전에는 풀리지 않았습니다.
  이제 공을 놓을 때 항상 실제 화면 안쪽으로 끌어옵니다.
  - 어쩌다 그런 자리에 놓이더라도 **스스로 화면 안으로 빠져나옵니다.** 모니터 구성이 바뀌거나
    다른 PC에서 공을 넘겨받는 등 다른 경로로 갇히는 것도 함께 막힙니다.

### 변경
- **슬라임 랜덤 반사 각도를 75° → 70°로 줄였습니다.** 던진 방향이 조금 더 살아 있습니다.

## [1.25.0] - 2026-08-25
### 추가
- **슬라임(젤리) 테마가 벽에 부딪히면 랜덤한 각도로 튕겨 나갑니다.** 지금까지는 당구공처럼
  입사각 그대로 반사돼서 던진 방향만 보면 갈 곳이 뻔했는데, 이제 반사 방향을 기준으로
  좌우 최대 **75°** 안에서 매번 다르게 튑니다. 같은 자리에 같은 각도로 던져도 매번 다른
  궤적이 나오고, **무한 튕기기**에서 두 지점만 왕복하며 갇히던 것도 없어집니다.
  - 던진 방향은 그대로 살아 있습니다. 비스듬히 던지면 그쪽으로 치우쳐 튕기고,
    정면으로 맞히면 좌우로 가장 넓게 퍼집니다.
  - 속도는 변하지 않습니다. 방향만 돌리기 때문에 **감속** 설정과 무한 튕기기의
    "힘을 얻지도 잃지도 않는다"는 동작이 그대로 유지됩니다.
  - 당구공·볼링공은 정직한 반사가 컨셉이라, 농구공·종이비행기는 중력으로 바닥에 착지해야 해서
    이 테마들에는 적용하지 않았습니다.

## [1.24.3] - 2026-08-25
### 변경
- **`ⓘ` 설명이 훨씬 빨리 뜹니다.** 마우스를 올리고 **1초** 기다려야 나오던 것을 **0.12초**로 줄였습니다.
  다른 항목으로 옮길 때는 기다림 없이 바로 뜨고, 설명이 사라지기까지의 시간도 5초에서 20초로
  늘려 긴 설명을 읽다가 끊기지 않습니다.

## [1.24.2] - 2026-08-25
### 수정
- **멀티 PC 탭의 `멀티 PC 사용` 옆에 `□□` 가 보이던 문제(1.24.1).** 항목 설명을 `ⓘ` 아이콘으로
  바꾸는 과정에서, **설명이 아니라 연결 상태를 보여 주는 자리**(`꺼짐`·`연결됨`)까지 아이콘으로
  바뀌었습니다. 아이콘 글꼴로 한글이 그려져 네모로 보였습니다. 원래대로 상태 문구로 되돌렸습니다.

## [1.24.1] - 2026-08-25
### 수정
- **설정 창 제목줄 위에 흰 가로줄이 생기던 문제(1.24.0).** 창 크기 조절을 켜면서 Windows 가
  창 위쪽에 밝은 리사이즈 프레임을 그렸습니다. 그 프레임을 없애고 크기 조절만 남겼습니다.

### 변경
- **항목 설명을 `ⓘ` 아이콘으로 옮겼습니다.** 설명이 항상 펼쳐져 있어 목록이 길고 복잡했습니다.
  이제 항목 이름 옆의 `ⓘ` 에 **마우스를 올리면** 설명이 뜹니다. 한 줄에 한 항목씩 보여
  훨씬 짧고 읽기 쉽습니다.
  - 탭 맨 위의 탭 설명은 그대로 둡니다.
  - 툴팁도 어두운 테마로 맞췄습니다.

## [1.24.0] - 2026-08-25
### 추가
- **설정 창 크기를 조절할 수 있습니다.** 모서리를 끌어 원하는 크기로 맞추세요(최소 900 x 560).
- **사이드바를 접을 수 있습니다.** 왼쪽 위 `☰` 를 누르면 목록이 **아이콘만** 남게 접혀
  설정 내용이 그만큼 넓어집니다. 다시 누르면 펴집니다.

### 변경
- **안내 문구가 훨씬 잘 보입니다.** 설명 글자가 배경과 너무 비슷해 흐릿했습니다.
  밝기를 올려 대비를 **4.8 : 1 → 7.1 : 1** 로 높였습니다.
  설정창뿐 아니라 업데이트 노트 본문·멀티 PC 상태 문구 등 보조 텍스트가 모두 함께 밝아집니다.
- **업데이트 진행바를 알약 모양으로 바꿨습니다.** 앞서 모서리를 둥글게 했지만 바 높이가 6px 이라
  거의 티가 나지 않았습니다. 높이를 **10px** 로 키우고 반지름을 절반으로 맞춰 확실히 둥글게 했습니다.

### 수정
- **단축키·멀티 PC·업데이트 노트 탭에서 설명 바로 밑에 패널이 붙어 있던 문제.**
  다른 탭과 같은 여백을 뒀습니다.
- **사이드바를 접었을 때 가로 스크롤바가 생기던 문제.**

## [1.23.0] - 2026-08-25
### 변경
- **설정 창을 카드형 UI 로 새로 만들었습니다.**
  - 설정을 **주제별 카드**로 묶고, 카드마다 아이콘과 제목을 달았습니다. 항목 사이에는 얇은 구분선을 넣어
    어디까지가 한 묶음인지 한눈에 보입니다.
  - **일반 탭은 2단**으로 나눴습니다 — 왼쪽 `모양/물리`·`동작`·`당구`, 오른쪽 `시스템`·`업데이트`,
    아래에 `초기화`. 예전처럼 한 줄로 길게 스크롤하지 않아도 됩니다.
  - **사이드바에 아이콘**을 넣고, 선택된 항목에 주황 막대를 표시합니다.
  - 창 크기를 **1100 x 740** 으로 넓혔습니다(기존 720 x 520). 2단 배치에 맞췄습니다.
  - `위치 초기화` · `설정 초기화` · `업데이트 노트 보기` 버튼에 아이콘을 넣고,
    되돌릴 수 없는 `설정 초기화` 는 주황 테두리로 구분했습니다.
  - 소리·단축키·멀티 PC·업데이트 노트 탭도 같은 카드 스타일로 맞췄습니다.

## [1.22.4] - 2026-08-25
### 변경
- **무한 튕기기 중에는 `Bounce Power` 도 잠깁니다.** 앞서 `감속` 만 막았는데, 반발력을 내려도
  벽에서 힘을 잃어 결국 멈추기 때문에 같은 문제였습니다. 이제 이 모드가 고정하는 값
  **두 가지(감속 0 · 반발력 100%)** 를 모두 잠그고, 각각 이유를 표시합니다.

## [1.22.3] - 2026-08-25
### 변경
- **업데이트 진행바 모서리를 둥글게 했습니다.** 창의 다른 요소와 각이 따로 놀던 것을 맞췄습니다.
  받는 중(진행률)과 적용 중(좌우로 흐르는 막대) 둘 다 둥급니다.
- **무한 튕기기 중에는 `감속` 을 조절할 수 없습니다.** 이 모드는 감속 0 을 전제로 하는데
  슬라이더로 올려 버리면 공이 결국 멈춰, "무한 튕기기가 켜져 있는데 멈춘다"는 앞뒤가 안 맞는
  상태가 됐습니다. 잠긴 동안 이유를 함께 보여 주고, 끄면 켜기 직전 값으로 돌아갑니다.

## [1.22.2] - 2026-08-25
### 수정
- **무한 튕기기에서 공이 던진 것보다 빨라지던 문제.** 벽에 부딪힐 때 회전(스핀)이 속도로 바뀌어
  더해지는데, 감속이 0 이라 그 이득이 **영구히 쌓였습니다.** 1500 으로 던져도 스핀 때문에
  **1966 까지 올라가 고정**됐습니다.
  - 이제 무한 튕기기에서는 스핀이 속도를 더하지 않습니다. **던진 속도가 그대로 유지**됩니다.
  - 평소 모드는 그대로입니다(스핀으로 튀는 맛은 유지).

> 무한 튕기기는 감속이 0 이라 **던진 속도가 끝까지 갑니다.** 잔잔하게 튕기게 하려면
> `던지기 가중치` 를 낮추세요 — 이 모드에서는 가중치가 곧 영구 속도입니다.

## [1.22.1] - 2026-08-25
### 변경
- **"날아가는 공이 마우스로 안 잡힌다" 원인을 찾기 위한 기록을 넣었습니다.** 동작은 그대로입니다.
  공이 움직이는 중에 좌클릭하면 로그에 다음이 남습니다.
  - 클릭이 **공 창까지 도달했는지**, 그때 **공까지 거리와 클릭 반경**은 얼마였는지
  - 전역 단축키 훅이 클릭을 **먼저 가로챘는지**
  - 설정 창이 열려 있었는지(열려 있으면 클릭 여유가 0 이 됩니다)

## [1.22.0] - 2026-08-25
### 추가
- **`무한 튕기기` 토글** (설정 → 일반 → 모양 / 물리). 켜면 공이 힘을 잃지 않고 계속 튕겨 다닙니다.
  - **감속 0 + 반발력 100%** 를 함께 맞춥니다. 감속만 0 으로 해서는 무한이 되지 않습니다 —
    벽에 부딪힐 때마다 반발 계수만큼 잃어 결국 멈추기 때문입니다.
  - **끄면 켜기 직전 값으로 정확히 돌아갑니다.**
  - 켜는 순간 **멈추는 방법을 알려 줍니다** — `잡기 단축키(내가 지정한 조합)를 누르면 언제든
    잡아서 멈출 수 있어요`. 사용자가 직접 지정한 단축키를 그대로 보여 줍니다.

### 변경
- **`감속` 최솟값이 0 까지 내려갑니다** (기존 0.2). 0 = 공기저항 없음.
- **`Bounce Power` 최댓값이 100% 로 올라갑니다** (기존 95%). 100% = 벽에서 힘을 잃지 않음.

> 무한 튕기기 중에는 공이 스스로 멈추지 않으므로 화면이 계속 갱신됩니다(노트북이면 배터리를 씁니다).
> 멈추려면 잡기 단축키로 잡거나, 공을 마우스로 잡거나, 일시 정지를 켜면 됩니다.

## [1.21.1] - 2026-08-25
### 수정
- **업데이트를 받아도 다시 켜면 또 받던 문제.** 교체 스크립트가 **복사 성공 여부를 보지 않고**,
  실패해도 받아 둔 파일을 지워 버렸습니다. 그래서 교체가 막힌 PC는 실행할 때마다
  **같은 파일을 처음부터 다시 받고**(160MB) 진행 팝업만 반복됐습니다.
  - 이제 **복사에 성공했을 때만** 받아 둔 파일을 지웁니다. 실패하면 그대로 남겨
    **다시 받지 않습니다.**
  - 실패한 버전은 **24시간 재시도를 보류**합니다. 보류 중에는 진행 팝업도 뜨지 않습니다
    (보류가 없으면 "실행 → 교체 실패 → 재시작" 이 무한히 돕니다).
  - **왜 안 되는지 한 번 알려 줍니다.** `업데이트를 적용하지 못했어요` 알림과 함께,
    백신 예외에 넣거나 쓰기 가능한 폴더로 옮기라고 안내합니다.
  - 로그에는 실패 코드와 **대상 폴더의 쓰기 가능 여부**까지 남습니다 — 권한 문제인지
    백신 문제인지 구분할 수 있습니다.
  - 24시간이 지나거나 **더 새로운 버전이 나오면** 자동으로 다시 시도합니다.

## [1.21.0] - 2026-08-25
### 변경
- **설정·로그·스킨을 `C:\ThrowMe` 한곳에 모읍니다.** 그동안 `%APPDATA%` 와 `%LOCALAPPDATA%` 로
  흩어져 있어 설정 파일도 로그도 찾기 어려웠습니다. 이제 앱을 켜면 **`C:\ThrowMe` 폴더가 자동으로
  만들어지고** 그 안에 전부 들어갑니다.
  - `settings.json` · `logs\` · `skins\` · `relay.json` · `update\`
  - **기존 사용자는 첫 실행에 자동으로 옮겨집니다.** 예전 폴더의 파일은 **지우지 않고 남겨 두므로**
    옛 버전으로 되돌아가도 설정을 잃지 않습니다. (확인 후 예전 폴더는 지우셔도 됩니다.)
  - 회사 정책 등으로 `C:\ThrowMe` 를 쓸 수 없으면 **예전 위치를 그대로 사용**하고 그 이유를
    로그에 남깁니다. 실패해도 아무것도 잃지 않습니다.
  - 실행 파일(exe)은 이번에는 옮기지 않았습니다. 자동 업데이트 경로와 얽혀 있어 다음 버전에서 따로 다룹니다.

### 추가
- **로그에 빠져 있던 항목을 채웠습니다.** 문의가 왔을 때 원인을 찾을 수 있도록 다음을 남깁니다.
  - **실행 환경** — 버전 · OS · 사용자 · exe 경로 · 데이터 폴더 · 모니터 구성
  - **업데이트 교체 결과** — 새 버전으로 실제로 바뀌었는지, 실패했다면 **대상 폴더에 쓰기가 되는지**까지
    (업데이트 팝업이 매번 뜨는 증상의 원인을 이걸로 확인할 수 있습니다)
  - **단축키 등록 성공/실패** — 다른 앱이 같은 조합을 먼저 쓰고 있으면 그대로 기록됩니다
- **로그 파일이 1MB 를 넘으면 `ThrowMe.1.log` 로 밀어내고 새로 씁니다.** 예전에는 회전이 없어
  파일이 계속 커졌습니다.

## [1.20.1] - 2026-08-25
### 수정
- **조합키 사이의 `+` 가 검게 나와 안 보이던 문제.** 흰색으로 바꿔 두 칸 사이가 또렷하게 보입니다.

### 변경
- **바람 단축키도 두 칸 형식으로 맞췄습니다.** 잡기·숨기기와 같은 `[수정자] + [키]` 모양입니다.
  왼쪽 `Ctrl` 칸은 **고정이라 누를 수 없고**(살짝 흐리게 표시), 오른쪽 키 칸만 바꿀 수 있습니다.

## [1.20.0] - 2026-08-18
### 변경
- **잡기·숨기기 단축키를 반드시 조합키로 지정합니다.** 칸이 **두 개**로 나뉘어,
  왼쪽에 수정자(`Ctrl`·`Shift`·`Alt`·`Win`), 오른쪽에 키 또는 마우스 클릭을 넣습니다.
  둘 다 채워야 저장됩니다.
  - 수정자를 두 개 함께 누르면(`Ctrl` + `Shift`) 둘 다 들어갑니다.
  - 예전에 수정자 없이 저장돼 있던 단축키는 **앱을 켤 때 `Ctrl` 을 붙여 고칩니다.**
    단독 키로 두면 평소 타이핑과 클릭까지 전역으로 가로챕니다.

### 추가
- **설정 창에서 `Ctrl` + `R` 로 설정 초기화.** 버튼과 똑같이 확인을 먼저 묻습니다.
  설정 창이 떠 있을 때만 동작합니다(다른 앱의 `Ctrl` + `R` 은 그대로 둡니다).

### 수정
- **숨겨 둔 공이 잡기 단축키만 눌러도 다시 나타나던 문제.** 잡기 단축키가 공을 불러오면서
  숨김까지 풀어 버렸습니다. 이제 **숨긴 상태에서는 잡기 단축키가 아무 일도 하지 않습니다.**
  다시 보이게 하는 건 숨기기 단축키(또는 설정의 `슬라임 표시`)만 할 수 있습니다.

## [1.19.1] - 2026-08-19
### 수정
- **비행 중 설정 창이 안 열리고 조작이 막히던 문제.** `항상 위` 때문에 슬라임 창이 최상위라
  설정 창을 가리고(안 열린 것처럼 보임), 날아가는 슬라임의 클릭 영역이 설정 창 클릭을 삼켰습니다.
  이제 설정 창을 최상위로 띄워, 비행 중에도 항상 앞에 뜨고 자유롭게 조작됩니다.
- **작업표시줄 때문에 위쪽 모니터에서 아래쪽 모니터로 못 넘어가던 문제.** 모니터 사이에 낀
  작업표시줄 높이만큼의 틈이 벽이 됐습니다. 충돌 판정을 전체 화면 영역 기준으로 바꿔,
  작업표시줄 틈을 지나 위·아래 모니터로 모두 넘어갑니다.

### 변경
- **사용법 힌트가 뜰 때마다 끄는 방법을 함께 안내합니다.** 힌트 제목에 `설정에서 끌 수 있어요` 를 붙였습니다.

## [1.19.0] - 2026-08-19
### 추가
- **사용법 힌트.** 이제 중간중간 우측 하단에 짧은 사용법을 순환하며 알려줍니다(약 3분 간격).
  던지기·클릭·스킨 변경·멀티 모니터·트레이 메뉴 사용법을 번갈아 보여줍니다.
  - 설정 → 일반 → `사용법 힌트` 토글로 켜고 끌 수 있습니다(기본 켜짐). 숨긴 상태에서는 뜨지 않습니다.

### 변경
- **날아가는 중에 바꾼 설정은 멈춘 뒤에 반영됩니다.** 공이 날아가는 동안 스킨이나 크기를 바꾸면
  비행이 끊겨 **설정이 안 먹거나 막힌 것처럼** 보였습니다. 이제 비행 중 바꾼 스킨·크기는
  공이 멈추는 순간 한 번에 적용됩니다(멈춰 있을 때 바꾸면 지금처럼 즉시 반영).

### 수정
- **4K·상하 배치 모니터에서 공이 경계에 막히던 문제.** 창은 실제 물리 픽셀로 움직이는데
  충돌 판정용 모니터 좌표는 다른 방식으로 얻어, 혼합 DPI(4K 배율)나 위·아래로 놓인 모니터에서
  좌표가 어긋나 **보이지 않는 벽/빈 틈**이 생겨 공이 넘어가지 못했습니다. 모니터 좌표를
  창 배치와 같은 실제 물리 픽셀로 직접 조회하도록 고쳐, 화면 사이를 제대로 넘어갑니다.

## [1.18.0] - 2026-08-14
### 추가
- **`최고 속도` 설정.** 던지기 가중치를 아무리 올려도 어느 지점부터 더 빨라지지 않던 이유는
  **속도 상한에서 잘리고 있었기 때문**입니다(가중치 7배쯤이면 툭 쳐도 상한에 닿습니다).
  이제 그 천장 자체를 `0.25배 ~ 4배` 로 조절할 수 있습니다. 올리면 지금보다 훨씬 빠르게 날아다닙니다.
  - 테마별 상한(종이비행기 · 농구공)에도 함께 곱해집니다. 어떤 테마를 쓰든 슬라이더가 먹습니다.
- **`감속` 설정.** 날아가던 공이 느려지는 정도를 `0.2배 ~ 3배` 로 조절합니다.
  **낮출수록 속도를 오래 유지해 멀리** 날아가고, 높이면 금방 멈춥니다.
  - 테마·골대·볼링장의 마찰에도 함께 적용됩니다.

> 둘 다 설정 → 일반 → `모양 / 물리` 에 있습니다. 기본값 1.00배는 지금까지와 완전히 같습니다.
> 던지기 가중치는 그대로 두었습니다 — 가중치는 `손놀림 대비 배율`, 최고 속도는 `천장` 으로 역할이 다릅니다.

## [1.17.1] - 2026-08-14
### 변경
- **일시 정지 중에 던지면 우측 하단에 알려줍니다.** `일시 정지` 가 켜져 있으면 공이 끌리기는 하는데
  놓아도 날아가지 않습니다(물리 계산을 멈춘 상태라 떨어지지도 않습니다). 화면에 아무 표시가 없어
  **고장으로 오해하기 쉬웠습니다.** 이제 그 상황에서 던지면
  `일시 정지 중이에요 — 그래서 놓아도 날아가지 않아요` 알림이 뜹니다.
  - 연달아 던져도 5초에 한 번만 뜹니다.
  - `알림 표시` 설정과 무관하게 뜹니다. 그 설정은 "공이 다른 PC로 넘어갔다" 알림용이고,
    이건 지금 조작이 왜 안 먹히는지에 대한 답이라 꺼 두면 다시 헤매게 됩니다.

## [1.17.0] - 2026-08-14
### 추가
- **설정 → 일반 → `설정 초기화`.** 설정이 꼬였을 때 **테마 · 물리(크기·반발·말랑함·던지기) ·
  소리 · 단축키 · 동작**을 한 번에 기본값으로 되돌립니다. 누르면 먼저 확인을 묻고,
  되돌린 뒤에는 공 위치까지 맞춰 줍니다. 앱을 다시 켤 필요 없이 바로 반영됩니다.
  - **지워지지 않는 것:** 멀티 PC 방 코드·비밀번호, 직접 그리거나 불러온 그림 파일.
    (그림을 쓰던 설정만 풀리므로, 테마에서 다시 켜면 그대로 씁니다.)
  - 기존 `위치 초기화` 는 그대로 옆에 있습니다.

## [1.16.2] - 2026-08-14
### 수정
- **공이 던져지지 않고 놓은 자리에 그대로 있던 문제.** `던지기 가중치` 가 **0** 으로 저장되면
  던질 때 속도가 `마우스 속도 × 0 = 0` 이라, 아무리 세게 휘둘러도 공이 손을 떠나지 않고
  놓은 자리에서 그냥 떨어졌습니다. 설정 파일에 저장된 값이라 앱을 다시 켜도 낫지 않았습니다.
  - **원인:** 멀티 PC 에서 방장의 `방 겉모습` 을 받을 때 가중치만 **검사 없이 그대로 적용**했습니다.
    (크기·이미지 배율은 원래부터 0 보다 클 때만 받고 있었는데 가중치만 빠져 있었습니다.)
    가중치 값이 빠진 메시지를 받으면 0 이 들어갑니다.
  - **고침 ①** 받는 값이 슬라이더 최솟값(0.3)보다 작으면 무시합니다.
  - **고침 ②** 이미 0 으로 저장된 설정은 **앱을 켤 때 자동으로 되돌립니다**
    (종이비행기는 0.3, 나머지 테마는 1.0). 테마·단축키·방 정보 등 **다른 설정은 그대로**입니다.

> 이미 이 증상을 겪고 있다면 앱을 껐다 켜기만 하면 됩니다. 설정을 지울 필요 없습니다.

## [1.16.1] - 2026-08-14
### 수정
- **공이 아예 잡히지 않던 문제(1.16.0).** 잡기 단축키만 바꾸면 될 것을 공의 클릭 처리까지
  건드리는 바람에, 맨 좌클릭은 막히고 `Ctrl + 좌클릭` 은 전역 단축키가 먼저 가져가면서
  양쪽 모두 잡을 수 없게 됐습니다. 공의 클릭 처리를 원래대로 되돌렸습니다.
  - **맨 좌클릭으로 잡고 던지기** — 1.15.1 과 동일하게 동작합니다.
  - **`Ctrl + 좌클릭`** — 커서 위치로 공을 불러오는 잡기 단축키.

## [1.16.0] - 2026-08-14
### 변경
- **잡기 단축키 기본값이 `Ctrl + 좌클릭` 으로 바뀌었습니다.**
  예전 설정 파일에는 수정자 없이 저장돼 있어, 업데이트 후 첫 실행에서 한 번 초기화합니다.
  그 뒤에 직접 바꾼 값은 유지됩니다.
- 잡기 단축키는 설정 → 단축키에서 **Ctrl 과 조합해 원하는 키·버튼으로** 바꿀 수 있습니다.

## [1.15.1] - 2026-08-14
### 수정
- **1.15.0 되돌림.** 공을 클릭 통과로 만든 방식이 좌클릭뿐 아니라 **우클릭까지 통과**시켜
  공 우클릭 메뉴(설정·위치 초기화·종료)를 열 수 없었습니다.

## [1.14.2] - 2026-08-14
### 수정
- **아이콘을 바꿔도 바탕화면 바로가기는 예전 그림으로 남던 문제.** 업데이트는 실행 파일을
  같은 자리에 덮어쓰기 때문에, 탐색기는 "바뀔 이유가 없다"고 보고 저장해 둔 옛 아이콘을
  계속 썼습니다. 이제 **버전이 바뀐 첫 실행에 아이콘 캐시를 갱신**하도록 알립니다.
  탐색기 재시작이나 로그아웃 없이 반영됩니다.

## [1.14.1] - 2026-08-14
### 변경
- **앱 아이콘을 종이비행기로 바꿨습니다.** 실행 파일(탐색기·작업표시줄·바로가기),
  트레이 아이콘, 창 아이콘에 모두 반영됩니다.
  16px 부터 256px 까지 9가지 크기를 담아 어느 크기에서도 선명합니다.

## [1.14.0] - 2026-08-12
### 변경
- **프로그램을 끄면 멀티 PC 도 함께 꺼집니다.** 종료할 때 방에서 나가고 `멀티 PC 사용` 을 해제해,
  다음에 켜면 **체크가 풀린 상태(방 밖)** 로 시작합니다.
  그동안은 방 정보가 그대로 남아 켜자마자 예전 방에 자동으로 다시 들어갔고,
  그래서 "끄고 다시 켰는데 이전 방으로 공이 넘어간다"는 문제가 있었습니다.
  (방 코드·비밀번호는 그대로 보관되므로, 다시 들어갈 때 입력할 필요는 없습니다.)
- **업데이트 노트를 5개씩 나눠 봅니다.** 버전이 쌓여 목록이 길어지던 것을 페이지로 나누고,
  아래에 `◀ 이전 / 1 / 3 / 다음 ▶` 이동을 넣었습니다.
- **방 겉모습을 참가자도 확인할 수 있습니다.** 멀티 PC 탭의 `방 겉모습` 에 방장이 정한
  **테마 · 던지기 가중치 · 반발력 · 말랑함 · 크기 · 그림 적용 여부**를 요약해 보여줍니다.
  참가자에게는 "방장(이름)이 정한 설정이 적용되어 있습니다" 로 안내합니다.

## [1.13.0] - 2026-08-12
### 변경
- **방장의 겉모습이 방 전체에 그대로 적용됩니다.** 방장이 `테마` 탭에서 고른
  **테마 · 가중치(던지기·반발·말랑함·크기) · 그림(불러온 이미지/그림판)** 이 방에 있는
  모든 PC에 똑같이 적용됩니다.
  - 예전에는 방장이 테마를 바꿔도 각자 자기 테마 그대로였습니다. `테마` 탭에서 고른 값은
    **로컬에만** 반영되고, 방으로 나가는 건 멀티 PC 탭의 별도 콤보뿐이었기 때문입니다.
    이제 그 콤보를 없애고 **방장의 테마 탭 설정이 곧 방 설정**이 됩니다.
  - 나중에 들어온 사람에게도 자동으로 적용됩니다(방장이 새 참가자를 보면 다시 보냅니다).
  - 그림은 최대 512px PNG 로 보관되며, 너무 크면(600KB 초과) 이미지만 건너뜁니다.
- **참가자는 `테마` 탭이 잠깁니다.** 어차피 방장 설정으로 덮어써지므로, 테마 카드와
  커스텀 이미지 영역을 비활성화하고 이유를 안내합니다. 방을 나가면 다시 풀립니다.

## [1.12.1] - 2026-08-12
### 수정
- **업데이트가 조용히 안 되던 문제(중요).** 같은 사무실의 여러 PC 는 **공인 IP 가 같습니다.**
  업데이트 확인에 쓰던 GitHub API 는 **IP 당 시간당 60회** 제한이 있어, 한도를 넘으면
  확인이 실패하는데 그 실패를 **아무 기록 없이 삼켜** 구버전에 계속 머물렀습니다.
  (실제로 이 때문에 1.11.4 에 갇혀 있었고, 그 버전의 설정창 오류도 계속 겪게 됐습니다.)
  - 이제 최신 버전 확인과 다운로드에 **API 를 쓰지 않습니다.** 릴리스 페이지의 리다이렉트와
    자산 직링크만 사용하며, 이 경로는 API 한도와 무관합니다.
  - 그래도 실패하면 **원인을 로그에 남깁니다**(한도 초과인지 네트워크인지 구분 가능).
  - 릴리스 노트 조회만 API 를 쓰지만, 실패해도 업데이트 자체는 정상 진행됩니다.
- **설정창이 아예 안 열리던 문제.** 파티원 목록을 그리다 오류가 나면 설정창 생성이 통째로
  실패했습니다. 이제 목록만 비워 두고 **설정창은 정상적으로 열립니다.**
- **트레이 메뉴에서 오류 대화상자가 뜨던 문제.** 트레이 콜백은 WPF 전역 예외 처리가 닿지 않아
  오류가 그대로 ".NET 오류 창"으로 노출됐습니다. 이제 감싸서 로그로만 남깁니다.

## [1.12.0] - 2026-08-12
### 수정
- **파티원 목록이 비어 있고, 공이 양쪽 PC에 동시에 생기던 문제.** 원인은 하나였습니다.
  파티원 카드를 만들 때 테두리 색으로 `DarkSeparator` 를 썼는데 이건 색(Brush)이 아니라
  **Style** 이라 형 변환 예외가 났습니다. 그 예외가 방 상태 처리를 타고 올라가
  **바로 뒤에 실행돼야 할 소유권 동기화까지 건너뛰게** 만들었고, 결국 두 PC 모두
  "내가 공 주인"인 상태로 남아 화면에 공이 둘 생겼습니다.
  - 색을 올바른 값으로 고쳤습니다.
  - 재발 방지: **소유권 동기화를 먼저** 실행하고, 설정창 갱신에서 나는 예외는 거기서 끊어냅니다.
    UI 문제로 공 관리가 깨지지 않습니다.
- **방 공통 테마 글씨가 검은색이라 안 보이던 문제.** 목록 항목에 어두운 테마 스타일을
  적용하지 않아 시스템 기본(검정)으로 나왔습니다.
- **볼링장·농구골대가 종료 후에도 화면에 남던 문제.** 정리 과정 중 한 곳에서 오류가 나면
  그 뒤 정리가 통째로 건너뛰어졌습니다. 이제 단계별로 따로 처리하고, 마지막에 **남아 있는 창을
  모두 닫는 안전망**을 둬서 어떤 테마든 공과 함께 정리됩니다.

### 변경
- **방장이면 `입장` 탭을 숨깁니다.** 내 방에 다시 입장하는 것처럼 보여 헷갈렸습니다.
- **종료할 때 방에서 정상적으로 나갑니다.** 예전에는 연결이 그냥 끊겨 서버가 뒤늦게 알아채는 바람에
  상대 파티 목록에 한동안 유령으로 남았습니다. 이제 나가면 즉시 사라지고 공 소유권도 곧장 넘어갑니다.
- **한 번에 하나만 실행됩니다.** 여러 개가 켜지면 공이 여러 마리로 보이고, 설정 파일을 서로
  덮어쓰며, 같은 이름으로 방에 중복 접속해 서로를 밀어냈습니다.
  (테스트용 `--profile` 을 지정한 경우에는 프로필별로 하나씩 허용합니다.)

## [1.11.4] - 2026-08-12
### 변경
- **나머지 창들도 같은 외곽으로 맞췄습니다.** `공에 그리기` 창과 업데이트 노트 팝업이
  설정창·진행창과 똑같은 둥근 모서리·1px 테두리·그림자로 보입니다. Windows 10 에서도 동일합니다.

> 슬라임·조준선·파티클·점수판처럼 화면에 겹쳐 그리는 창들은 배경이 투명해야 해서 대상이 아닙니다.
> 알림(토스트)은 서서히 사라지는 효과 때문에 투명 창이어야 하지만, 이미 같은 모양(둥근 모서리 +
> 1px 테두리 + 그림자)이라 그대로 뒀습니다.

## [1.11.3] - 2026-08-12
### 수정
- **Windows 10 에서 창 모서리가 각지던 문제.** 1.11.2 에서 창 외곽을 OS(DWM) 에 맡기면서,
  둥근 모서리가 Windows 11 에서만 나오고 Windows 10 에서는 각지게 바뀌었습니다.
  OS 를 보고 갈라지도록 고쳤습니다 — Windows 11 은 OS 그림자를 그대로 쓰고,
  Windows 10 은 1.11.1 까지 쓰던 방식(앱이 직접 둥글게 자르기)으로 되돌아갑니다.
  두 경우 모두 둥근 모서리 + 같은 톤의 1px 테두리로 보입니다.

## [1.11.2] - 2026-08-12
### 변경
- **창 외곽을 Windows(DWM) 가 직접 그리도록 바꿨습니다.** 설정창 그림자가 두껍고 진해서
  튀어 보였던 것을, 다른 앱 창과 똑같은 OS 기본 그림자로 바꿨습니다. 모서리 둥글기도
  Windows 11 기본값을 따르고, 테마와 같은 톤의 1px 테두리(#3C3C44)가 생겼습니다.
- **업데이트 진행창도 같은 외곽으로 맞췄습니다.** 설정창과 똑같이 둥근 모서리·테두리·그림자가
  붙습니다.

> Windows 10 에서는 이 OS 기능이 없어 각진 모서리로 보였는데, 1.11.3 에서 바로잡았습니다.

## [1.11.1] - 2026-08-12
### 수정
- **설정창 모서리가 사각형으로 보이던 문제.** 둥근 모서리 밖으로 타이틀바·사이드바 배경이
  삐져나와 네 귀퉁이가 각져 보였습니다. 창 외곽을 둥근 영역으로 직접 잘라내 실제 화면에서도
  네 모서리가 깔끔하게 둥글게 보이도록 고쳤습니다. 그림자도 둥근 외곽선을 따라갑니다.

## [1.11.0] - 2026-08-12
### 추가
- **설정에 `업데이트 노트` 탭.** 버전별 릴리스 노트를 최신순으로 쭉 보여줍니다.
  지금 쓰는 버전에는 `사용 중` 표시가 붙습니다. `새로고침` 으로 다시 받아올 수 있습니다.
  (기존 `최근 변경 내용 보기` 버튼과 팝업 창은 이 탭으로 대체됐습니다.)
- **공 우클릭 메뉴에 현재 버전 표시.** 메뉴 아래쪽에 `ThrowMe vX.Y.Z` 가 보입니다.

## [1.10.1] - 2026-08-12
### 수정
- **업데이트 진행창 문구가 잘리던 문제.** "vX.Y.Z 로 업데이트하고 있습니다. 끝나면 자동으로
  다시 시작합니다." 를 한 줄에 넣어 창 폭에서 잘렸습니다. 두 줄로 나눠 보여줍니다.

## [1.10.0] - 2026-08-12
### 변경
- **업데이트가 조용해졌습니다.** 앱을 켤 때 새 버전이 있으면 **묻지 않고** 진행바만 잠깐 띄워
  받아서 적용하고 자동으로 다시 시작합니다. "지금 재시작할까요?" 확인창과 업데이트 직후
  변경 내용 팝업을 없앴습니다.
- **변경 내용은 설정에서 봅니다.** 설정 → 일반 → `최근 변경 내용 보기`.
  업데이트 직후에는 방금 적용된 버전의 노트를 보여주고, 그 외에는 최신 릴리스 노트를 가져옵니다.
  관련 설정 토글(`변경 내용 알리기`, `새 버전 받으면 바로 물어보기`)은 필요 없어져 제거했습니다.

### 수정
- **다른 PC로 던진 공이 사라져 앱을 다시 켜야 하던 문제.**
  공을 넘길 때 클라이언트는 공을 숨기고 물리를 멈춘 뒤 서버 결과를 기다리는데, 서버가 소유자로
  인정하지 않으면 `HANDOFF_RESULT` 대신 **`ERROR` 만** 보냈고 클라이언트는 그걸 로그만 찍고
  무시했습니다 → 결과를 영원히 기다리며 공이 사라졌습니다.
  - 서버: `NOT_OWNER` 일 때도 `HANDOFF_RESULT(accepted=false)` 를 함께 보냅니다.
  - 클라이언트: `ERROR` 를 받으면 대기 중이던 공을 즉시 회수하고, **6초 워치독**으로
    응답이 아예 없어도 공을 되돌립니다. 회수 위치도 유효성을 검사해 작업표시줄에 갇히지 않습니다.
- **접속하지 않은 PC로 향하는 배치가 만들어지던 문제.**
  서버의 파티 순서는 나간 사람도 계속 보관하는데 방장이 그걸로 배치를 만들어, 방에 없는 PC 로
  던지는 통로가 생겼습니다. 이제 **현재 접속 중인 멤버만으로** 배치를 만들고,
  `relay.json` 에 남아 있던 예전 방의 링크도 걸러냅니다.
- **멀티 PC 진단이 불가능하던 문제.** 릴레이가 실패만 기록해 로그로 원인을 볼 수 없었습니다.
  이제 방 상태(방장·순서·접속자)와 핸드오프 송수신·회수 사유를 남깁니다.

## [1.9.2] - 2026-07-29
### 변경
- **설정창을 여는 동안에는 공 주변 클릭 여유를 없앱니다.** 공 근처를 넉넉히 클릭할 수 있게 해 둔
  여유(기본 28px)가, 공이 날아갈 때 보이지 않는 원이 되어 뒤에 있는 설정창의 클릭을 가렸습니다.
  이제 설정창이 열려 있는 동안에는 **공 그림만큼만** 클릭을 받습니다.
  설정창을 닫으면 원래 여유로 돌아옵니다.

## [1.9.1] - 2026-07-29
### 변경
- **재시작을 묻기 전에 변경 내용을 먼저 보여줍니다.** 예전에는 "새 버전 받았습니다, 지금 재시작할까요?"
  만 묻고, 무엇이 바뀌는지는 재시작한 뒤에야 알 수 있었습니다. 순서가 거꾸로였습니다.
  이제 **확인창에서 바로 변경 내용을 읽고** 재시작 여부를 정할 수 있습니다.
  여기서 이미 읽었다면 재시작 후 같은 내용이 또 뜨지 않습니다.

## [1.9.0] - 2026-07-29
### 수정
- **업데이트 후 변경 내용 팝업이 뜨지 않던 문제.** 팝업을 "할 일이 없을 때"(ApplicationIdle) 띄우도록
  예약했는데, 공이 움직이는 동안에는 물리 루프가 계속 돌아 그 시점이 오지 않았습니다.
  그래서 업데이트 직후 공을 받아 움직이기 시작하면 팝업이 영영 뜨지 않았습니다.
  이제 짧은 타이머로 확실히 띄웁니다.
- **종료 중 오류가 반복 기록되던 문제.** 앱을 닫는 도중 다른 PC에서 공이 넘어오면
  이미 닫힌 창을 다시 띄우려다 오류가 났습니다.

### 변경
- **여러 버전을 건너뛰면 그 사이 내용을 모두 보여줍니다.** 예전에는 최신 릴리스 하나의 내용만
  담겨서, 1.5.0 에서 1.8.0 으로 한 번에 올라가면 1.6.0·1.7.0 의 변경 내용이 사라졌습니다.
  이제 건너뛴 버전들을 최신순으로 모아 한 팝업에 보여줍니다.

## [1.8.0] - 2026-07-28
### 수정
- **공이 날아가는 동안 다른 창이 클릭되지 않던 문제.** 공은 112px 인데 창은 그 4배(446px)였고,
  넓게 낚아채려고 **창 전체가 클릭을 받도록** 되어 있었습니다. 그래서 보이지 않는 사각형이
  공을 따라다니며 뒤에 있는 창의 클릭을 삼켰습니다(설정창 탭이 안 눌리는 증상).
  이제 **공 주변 원형**으로만 클릭을 받습니다. 범위는 설정 → 일반 → `클릭 여유` 에서 조절합니다.

### 추가
- **날아가는 공을 클릭하면 되치기.** 예전에는 움직이는 공을 클릭하면 그 자리에서 멈췄습니다.
  이제 **속도를 유지한 채 클릭한 반대 방향으로 튕겨** 나갑니다. 라켓으로 치듯 방향을 바꿀 수 있고,
  빠른 공을 되칠수록 이펙트도 커집니다.
- **작업표시줄에 표시.** 작업표시줄과 Alt+Tab 목록에 ThrowMe 가 나옵니다.
  설정 → 일반 → `작업표시줄에 표시` 로 끌 수 있습니다.
- **공이 넘어갈 때 우측 하단 알림.** 공이 다른 PC로 넘어가면 **누구에게 갔는지** 알려줍니다.
  그동안은 공이 상대 PC로 가면 화면에서 그냥 사라져서 앱이 죽은 것처럼 보였습니다.
  공이 돌아올 때도 알려줍니다. 설정 → 일반 → `알림 표시` 로 끌 수 있습니다.

## [1.7.0] - 2026-07-28
### 추가
- **앱 아이콘.** 보라 배경에 날아가는 민트 슬라임 아이콘을 넣었습니다.
  실행 파일(탐색기·작업표시줄·바로가기), 트레이 아이콘, 설정창 등 창 아이콘에 모두 적용됩니다.
  - 16 · 20 · 24 · 32 · 40 · 48 · 64 · 128 · 256px 프레임을 담아 어느 크기에서도 선명합니다.
  - 원본 이미지의 둥근 모서리 바깥이 검정이라 그대로 쓰면 작업표시줄에 검은 사각형이 보입니다.
    둥근 사각형 알파 마스크를 씌워 모서리를 투명하게 처리했습니다.
  - 리소스를 못 읽는 경우에만 기존처럼 코드로 그린 아이콘으로 대체합니다.

### 수정
- csproj 주석의 한글이 깨져 있던 것을 고쳤습니다(동작 영향 없음).

## [1.6.0] - 2026-07-27
### 변경
- **앱 이름이 `Slimey` → `ThrowMe` 로 바뀌었습니다.** 창 제목·트레이 아이콘·설정창 등
  눈에 보이는 모든 이름과, 내부 네임스페이스·어셈블리·실행 파일명(`ThrowMe.exe`)까지 함께 바꿨습니다.
- **설정·커스텀 스킨·방 접속 정보는 그대로 유지됩니다.** 데이터 폴더도
  `%APPDATA%\ThrowMe`, `%LOCALAPPDATA%\ThrowMe` 로 옮겼지만, 첫 실행 때 예전 폴더가 있으면
  자동으로 복사해 옵니다(`AppPaths`). 예전 폴더는 지우지 않으므로 문제가 생겨도 원본이 남습니다.
- **멀티 PC 연결은 영향받지 않습니다.** 릴레이 서버 주소(`slimey-relay.throwme.workers.dev`)는
  그대로 두었습니다 — 바꾸면 아직 업데이트하지 않은 PC 와 연결이 끊기기 때문입니다.

## [1.5.1] - 2026-07-27
### 수정
- **업데이트 알림 팝업에서 인용문이 깨져 보이던 문제.** 릴리스 노트의
  `> ⚠️ ...` 안내 문구가 해석되지 않아 꺾쇠 기호가 글자로 그대로 노출됐습니다.
  이제 왼쪽에 강조색 세로선을 둔 안내 블록으로 표시합니다.
- **여러 줄로 쓴 항목이 목록 밖으로 떨어져 나오던 문제.** 한 항목을 두세 줄로
  접어 쓰면 이어지는 줄이 별도 문단이 되어 글머리표 밖에 왼쪽 정렬로 붙었습니다.
  이제 해당 항목 본문으로 이어 붙습니다.

## [1.5.0] - 2026-07-27
### 추가
- **방장 위임.** 파티원 카드를 **우클릭 → 방장 위임**. 접속 중인 사람에게만 넘길 수 있습니다.
  위임한 뒤에는 방을 만든 사람이 다시 들어와도 방장을 자동 회수하지 않습니다(위임 의사 존중).
- **방 공통 테마.** 방장이 `방 공통 테마`를 고르면 **방에 있는 모든 PC의 테마가 같이 바뀝니다.**
  나중에 입장한 사람도 들어오는 즉시 방 테마로 맞춰집니다.

### 변경
- **배치를 도형으로 표시.** 파티원을 세로 목록 대신 **모니터 카드가 좌 → 우로 이어진 형태**로
  보여줍니다. 카드 사이의 `↔` 선이 공이 지나가는 통로이고, 공을 가진 PC의 카드에는 점이 표시됩니다.
  드래그로 순서를 바꾸는 방식은 그대로입니다.

### 수정
- **방을 만든 사람이 방장을 빼앗기던 문제.** 개설자의 연결이 잠깐 끊기면(앱 재시작·네트워크 순단)
  방장이 다른 사람에게 승계된 뒤 돌아와도 되돌아오지 않았습니다. 이제 서버가 **개설자를 따로 기억**해
  돌아오면 방장을 회복시킵니다(명시적으로 위임한 경우는 제외).
- **모두 나간 뒤에도 파티원 목록에 사람이 남던 문제.** 방을 나갈 때 멤버·순서·방장 정보를 비웁니다.
- **공이 없는 상태로 방을 나가면 공이 돌아오지 않던 문제.** 공을 남에게 넘긴 직후에는 공 좌표가
  화면 밖(넘어가던 위치)이라, 방을 나가며 공을 되찾아도 화면 밖에 놓여 보이지 않았고 물리도 멈춰
  앱을 다시 켜야 했습니다. 이제 되찾을 때 **위치가 유효한지 검사해** 화면 밖이면 중앙으로 되돌립니다.
  서버 연결이 끊길 때도 같은 처리를 합니다.
- **공이 빠를 때 렉이 걸리던 문제.**
  - 창 이동을 WPF `Left/Top` 대신 **Win32 `SetWindowPos`** 로 바꿨습니다. 투명(레이어드) 창에서
    `Left/Top` 세터는 의존 속성 변경 → 레이아웃 → 재합성을 매 프레임 유발합니다. 위치가 그대로면
    아예 호출하지 않습니다.
  - 파티클·타격 문구 오버레이가 **표시할 게 없어도 매 프레임 풀 전체를 순회**하던 것을 조기 반환으로
    막았습니다. 공이 움직이는 내내 렌더 루프가 도는 만큼 누적 비용이 컸습니다.

## [1.4.0] - 2026-07-27
### 추가
- **업데이트 후 변경 내용 팝업.** 자동 업데이트가 적용되어 새 버전이 처음 실행될 때,
  무엇이 달라졌는지 알려주는 팝업이 한 번 뜹니다. 그동안은 조용히 교체만 되어서
  사용자가 새 기능이 생긴 줄 모르고 지나가는 문제가 있었습니다.
  - 내용은 **GitHub 릴리스 노트 본문**을 그대로 보여줍니다. 업데이트 확인 시 이미 받아오던
    응답에 들어 있어 추가 통신이 없습니다.
  - 제목(`##`)·불릿(`-`, 중첩 포함)·**굵게**·인라인 코드 정도의 마크다운을 해석합니다
    (외부 마크다운 패키지 없음).
  - **적용된 버전과 실행 중인 버전이 같을 때만** 뜨고, 한 번 보면 다시 뜨지 않습니다.
- **설정 → 일반 → 업데이트**에 `변경 내용 알리기` 토글과 `최근 변경 내용 보기` 버튼 추가.
  팝업 안에서도 `다음부터 알리지 않기`로 끌 수 있고, 끈 상태여도 노트는 소비되어
  다음 업데이트 때 옛 내용이 뜨지 않습니다.
## [1.3.1] - 2026-07-27
### 수정
- **골이 들어갔는데 득점이 안 되던 문제 수정(농구).** 원인이 세 개 겹쳐 있었습니다.
  - **판정 X 좌표를 프레임 끝에서 읽었음.** 공이 림 높이를 지나는 *순간*이 아니라 그 프레임이
    끝난 위치로 폭을 재고 있었습니다. 그래서 좌우 속도가 빠른 슛(비스듬히 넣는 슛)은
    판정 시점에 공이 이미 개구부를 지나쳐 있어 골이 누락됐습니다. 정중앙으로 깨끗이 넣은
    슛조차 빠르면 탈락할 수 있었습니다. → 림 높이 교차 지점의 X 를 보간해서 판정합니다.
  - **`vel.Y > 0`(하강 중) 조건이 림 충돌에 뒤집혔음.** 림을 살짝 스치며 들어간 공은
    통과 프레임에서 속도가 위로 반사되어 조건이 깨졌습니다. → 위치 변화량(dy)으로 하강을
    판정하므로 스치고 들어간 골도 인정됩니다.
  - **인정 폭이 너무 좁았음(0.17S).** 겹침 없이 지나는 이론폭(≈0.18S)에 맞춰져 있었는데,
    빠른 공은 프레임 단위 이동 탓에 림 높이에서 이미 살짝 겹친 상태로 통과합니다(눈에는
    들어간 골). → 공 표면이 림 튜브 중심에 닿는 지점까지, **0.24S** 로 넓혔습니다.
  림에 맞고 튀어나가는 공은 교차 지점 X 가 개구부 밖이라 여전히 득점으로 세지 않습니다.

## [1.3.0] - 2026-07-27
### 추가
- **빈 방 자동 폐기(서버).** 방에 아무도 없는 상태가 **30분** 지나면 방을 폐기합니다
  (시크릿·파티 순서·배치·소유권 전부 삭제). 그 전에는 방이 영구히 남아 있어서
  ① 한 번 정한 비밀번호를 바꿀 수 없고 ② 예전에 접속했던 PC 이름이 파티 순서에 계속
  쌓이는 문제가 있었습니다. 앱을 껐다 켜는 정도(30분 이내)로는 방이 유지됩니다.
  ※ Durable Object 는 알람이 하나뿐이라, 핸드오프 ACK 타임아웃과 폐기 예약 중
  **더 이른 마감시각**으로 재예약하도록 알람을 통합했습니다.
- **방장(호스트) 시스템.** 방 코드를 처음 만든 PC가 **방장**이 되고, **배치는 방장만** 정합니다.
  다른 PC가 순서·배치를 바꾸려 하면 서버가 `NOT_HOST` 로 거부합니다. 방장이 나가면
  파티 순서상 다음 노드로 자동 승계됩니다(방장 없는 방이 생기지 않도록).
- **멀티 PC 설정에 탭 추가 — `방 생성` / `입장`.**
  - 방 생성: 방 코드(자동 생성 버튼 포함)·비밀번호·PC 이름 → 내가 방장이 됩니다.
  - 입장: 방장에게 받은 방 코드·비밀번호·PC 이름을 입력해 참여합니다.
- **파티원 목록.** 방에 들어오면 참여자가 표시되고, **방장이 드래그로 좌 → 우 순서**를
  정합니다. 목록에 방장·공 소유 표시가 함께 나옵니다.

### 변경
- **배치를 좌우 전용으로 단순화.** 위/아래 매핑과 엣지별 수동 설정 UI를 없애고,
  **파티 순서 하나**로 배치를 정하도록 바꿨습니다. 순서 [A, B, C] 는
  `A.Right↔B.Left`, `B.Right↔C.Left` 체인으로 자동 변환됩니다(양 끝은 벽에 튕김).

### 수정
- **다른 PC로 넘어온 공이 작업표시줄에 붙어 멈추던 문제 수정.**
  진입 위치를 엣지 법선 방향으로만 밀어 넣어서, 진입 지점이 화면 위/아래 끝에 가까우면
  공 절반이 경계 밖(작업표시줄·화면 밖)에 놓였습니다. 그러면 물리 엔진이 모든 방향을 벽으로
  판정해 그 자리에 갇혔습니다. 이제 ① 접선 방향으로도 공 반지름만큼 여유를 두고,
  ② **작업영역 기준 엄격 판정**으로 유효한 자리를 찾아(없으면 안쪽으로 더 들어가며 탐색)
  공을 놓습니다. 놓을 자리가 전혀 없으면 수락을 거부해 보낸 쪽이 반사합니다(공 유실 없음).

## [1.2.0] - 2026-07-27
### 추가
- **릴레이 서버 배포 완료.** 멀티 PC 공 전달이 실제 인터넷 경유로 동작합니다.
  Cloudflare Workers + Durable Objects, 무료 티어. 방 생성·시크릿 인증·프레즌스·
  핸드오프 라우팅·소유권 검증(비소유자 거부)까지 서버 검증 10/10 통과.

### 변경
- **서버 주소를 앱에 내장**(`AuthService.DefaultServerBaseUrl`). 설정 창에서 서버 주소
  입력란을 없앴습니다 — 모든 PC가 같은 값을 쓰는데 매번 입력하면 오타 위험만 커지기 때문입니다.
  이제 멀티 PC 설정은 **방 코드 + 시크릿 + 이 PC 이름**만 입력하면 됩니다.
  개발용 로컬 서버(`ws://127.0.0.1:8787`)로 바꾸려면 `relay.json` 의 `serverBaseUrl`
  또는 실행 인자로 덮어쓸 수 있습니다(UI에는 노출하지 않음).

## [1.1.1] - 2026-07-27
### 수정
- **타격 문구·파티클이 엉뚱한 곳(화면 한복판)에 뜨던 문제 수정.**
  이펙트를 물리 계산이 **끝난 뒤**의 공 위치에 띄우고 있어서, 공이 빠를수록 실제 벽 접점에서
  크게 빗나갔습니다(최고속에서 한 프레임 이동량 = 최대 2000px). 이제 물리 엔진이 **충돌이 일어난
  순간의 위치**(`PhysicsStepResult.CollisionPosition`)를 함께 돌려주고, 문구·파티클·쿠션 스파크가
  모두 그 지점에 발생합니다.
- **멀리 떨어진 이펙트가 잘려 안 보이던 문제 수정.** 문구/파티클 오버레이는 성능을 위해
  작은 고정 크기 창 하나로 그리는데, 창을 활성 이펙트들의 **무게중심**에 놓고 있어서 공이 양쪽 벽을
  번갈아 때리면 창이 그 사이(화면 한복판)로 가버려 양쪽 이펙트가 모두 창 밖으로 잘렸습니다.
  → 창을 **경계 중심**에 놓고, 새 이펙트가 창 범위를 벗어나게 만들면 오래된 것을 비워
  "활성 이펙트는 항상 창 안"을 보장합니다. (창 리사이즈는 레이어드 창에서 동기 스톨을 유발하므로 쓰지 않음.)

### 변경
- **던지기 가중치 범위 확대** 10.0x → **20.0x**. 속도 상한도 함께 상향(`MaxThrowSpeed`/`MaxSpeed` 20000 → 40000 px/s).
  최고속에서도 substep 터널링 방지가 유지됨을 확인(400프레임 × 40000px/s 전부 화면 안).

## [1.1.0] - 2026-07-27
### 추가
- **빠르게 숨기기 단축키**(기본 `Alt + \``) — 슬라임·놓아둔 당구공·이펙트 오버레이를 한 번에 감추고,
  같은 키를 다시 누르면 되돌아옵니다. 설정창 '단축키' 탭에서 키/마우스 조합으로 재설정 가능.
- **테마별 커스텀 이미지** — 슬라임·당구공 테마에 내 이미지를 덧씌울 수 있습니다.
  - **불러오기**: PNG/JPG 등을 고르면 앱 저장소로 복사해 보관(원본을 옮기거나 지워도 안 깨짐).
  - **그리기**: 앱 안에서 직접 그려 공에 올림(10색 팔레트·굵기·지우개·되돌리기·배경 투명/흰색).
  - 이미지는 공 원형으로 잘리고 구면 음영·광택이 덧씌워져 스티커가 아니라 공 표면처럼 보입니다.
  - 테마별로 따로 저장되며, 덧씌우기 on/off 와 이미지 크기(30~160%)를 조절할 수 있습니다.
  - 몬스터볼·하이퍼볼·마스터볼은 고유 디자인 유지를 위해 제외.

### 변경
- **던지기 가중치 범위 확대** 2.0x → **10.0x**. 실제로 체감되도록 속도 상한도 함께 상향
  (`MaxThrowSpeed`/`MaxSpeed` 7000 → 20000 px/s).
- 충돌 이펙트(소리·파티클·찌그러짐·표정) 기준값을 속도 상한과 **분리**(`ImpactReferenceSpeed` = 7000).
  상한을 올려도 이펙트가 터지는 임계는 이전과 동일하게 유지됩니다.

## [1.0.2] - 2026-07-24
### 수정
- 3/4구를 놓은 상태로 테마를 바꾸면 당구공이 안 치워지던 문제 수정 — 테마 변경 시 함께 정리.
- 설정창 스크롤바 재디자인(얇고 둥근 썸, 화살표 제거) + 모서리 곡률 정리.
- 일반 탭 '위치 초기화' 버튼 크기를 여유있게 조정.
- UI 폰트를 Pretendard(맑은 고딕 폴백)로 변경.

## [1.0.1] - 2026-07-24
### 변경
- 저장소를 공개(Public)로 전환 — 누구나 `git clone` 가능.
- 자동 업데이트를 **토큰 없이** 동작하도록 정리(공개 릴리스 직링크로 다운로드). 배포 exe에 토큰을 넣지 않음.

## [1.0.0] - 2026-07-24
### 최초 릴리스
- **데스크톱 슬라임**: 투명 창의 슬라임을 드래그·던지기, 관성/마찰/모서리 반사(당구 쿠션식), 멀티 모니터(음수 좌표·간격·DPI) 대응.
- **스킨**: 젤리 / 당구공(기본 8번 검정, 4·3구 시 흰 수구) / 몬스터볼·하이퍼볼·마스터볼(클릭 열림).
- **스핀**: 드래그 컬로 사이드 스핀(마그누스 곡선) 충전 — 드래그 중 유지, 던질 때 소모.
- **큐대 모드**(당구공): 공 주위 클릭→당겨서 발사. 스핀 점으로 세로=끌어치기/밀어치기, 가로=사이드.
  - 아래(6시) 끌어치기 = 앞으로 갔다가 되돌아옴, 위(12시) 밀어치기 = 더 밀고 나감.
- **타격 문구**: 젤리가 벽에 부딪히거나 맞으면 "Hit!/Pow!/Bonk!" 등 메이플식 문구가 팝업.
- **잡기 단축키**: 전역 단축키/마우스 버튼으로 슬라임을 커서로 회수(기본 Ctrl+Shift+G).
- **설정 창**: 다크 2-pane, 크기/반발/말랑/던지기·테마 카드·소리·단축키.
- **자동 업데이트**: 실행 시 새 릴리스가 있으면 백그라운드로 받아 다음 실행 때 조용히 적용.
